#include<iostream>
using namespace std;
int main()
{
  //Type your code here.
  int r,x;
  cin>>r>>x;
  if(2*r<=x)
    cout<<"circle can be inside a square";
  else
    cout<<"circle cannot be inside a square";
  return 0;
}